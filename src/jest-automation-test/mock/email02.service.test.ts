// import { sendEmail } from "../mock/email02.service";
// jest.mock(`./email02.service`);
// test(`Gửi email`, () => {
//   sendEmail(`ngoc@gmail.com`, `Hello`);
//   // Kiểm tra hàm đã được gọi với tham số đúng
//   expect(sendEmail).toHaveBeenLastCalledWith(`ngoc@gmail.com`, `Hello`);
//   // Kiểm tra hàm đã được gọi đúng 1 lần
//   expect(sendEmail).toHaveBeenCalledTimes;
// });
