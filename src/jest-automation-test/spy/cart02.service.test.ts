// import * as cartService from "../spy/cart02.service";
// test(`Kiểm tra hàm được gọi`, () => {
//   const spy = jest.spyOn(cartService, `calculateTotal`);
//   const total = cartService.calculateTotal([10, 20, 30]);
//   // Kiểm tra xem hàm đã được gọi
//   expect(spy).toHaveBeenCalled();
//   // Kiểm tra tham số truyền vào
//   expect(spy).toHaveBeenCalledWith([10, 20, 30]);
//   // Kiểm tra kết quả trả về
//   expect(total).toBe(60);
// });
